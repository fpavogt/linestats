# This workflow will push the code onto pypi.
# It uses the PYPI_USERNAME and PYPI_PASSWORD secrets from GITHUB to login on there.
#
# Copyright (c) 2022 MeteoSwiss, created by F.P.A. Vogt; frederic.vogt@meteoswiss.ch

name: CI_pypi

on:
  release:
    types: [published]
  pull_request:
    branches: [ master, develop ]

jobs:
  pypi:

    runs-on: ubuntu-latest

    steps:

    - name: Checkout current repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.10'

    - name: Install dependancies
      run: |
        python -m pip install --upgrade pip
        pip install setuptools
        pip install wheel
        pip install twine
      shell: bash

    - name: Build, testpypi, and pypi
      # Let's make use of Github secrest at the repo level to avoid spelling out secret stuff
      env:
        PYPI_USERNAME: ${{ secrets.PYPI_USERNAME }}
        PYPI_PWD: ${{ secrets.PYPI_PASSWORD }}
      # After assembling the distribution, we first go to testpypi to make sure nothing blows up
      # Only then do we push to pypi
      run: |
        python setup.py sdist bdist_wheel
        twine upload -r testpypi dist/* --verbose --skip-existing -u "$env:PYPI_USERNAME" -p "$env:PYPI_PWD"
      shell: bash
